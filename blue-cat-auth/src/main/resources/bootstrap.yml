spring:
  application:
    name: blue-cat-auth
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        prefix: blue-cat-auth
  profiles:
    active: dev