spring:
  application:
    name: blue-cat-gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        prefix: blue-cat-gateway
  profiles:
    active: dev